
location 0 has a single choice (+) between two steps (execute).
location 0 runs either the first step or the second step, not both.
both steps take the same input dependency.
no sends or receives — all execution stays on a single location.


python tmp_main.py compile --outdir build examples/local_choice/source.swirl examples/local_choice/config.yml